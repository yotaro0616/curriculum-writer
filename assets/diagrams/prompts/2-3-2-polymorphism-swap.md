# 2-3-2-polymorphism-swap

**モデル**: pro | **アスペクト比**: 16:9 | **解像度**: 4k

## プロンプト本文

```
横長の比喩インフォグラフィックを作成してください。

【内容】
- テーマ: ポリモーフィズム。同じソケット（インターフェース型）に違う実装プラグを差し替える。同じ呼び出しで、実体によって振る舞いが変わる。
- 中央に機器を1つ描く。機器のラベル「TaskService」。機器の前面に1つのソケット（差込口）があり、ソケットのラベル「NotificationSender 型」。
- そのソケットの手前に、同じ差込形状で交換できる3つのプラグを並べる。どれでも挿せることを示す。1つ目のラベル「メール」、2つ目「Slack」、3つ目「テスト用ダミー」。プラグの差し替えを示す両矢印を添える。オレンジ #F97316。
- 機器から吹き出しを1つ出し、「send() の呼び出しは同じ。出力だけ変わる」と記す。
- 画面下部の余白に、結論の一文を大きく置く。文言は「呼び出しは同じ、中身は差し替え自由（本番は本物・テストはダミー）」。
- 要素は7個以内に抑える。

【スタイル】
- フラットデザイン・インフォグラフィック風、ソケットと差込プラグのシンプルなアイコン
- 背景は完全な純白（#FFFFFF）。影・グラデーション・テクスチャ・かすれを一切入れない、クリーンでシャープなベクター線画
- ソケットと機器は青 #3B82F6、差し替えるプラグはオレンジ #F97316 のアクセント
- ラベルは日本語、文字は大きく最小限
- 16:9 横長レイアウト
```

## 生成日時

2026-06-03
