# sample-polymorphism-ticket

**モデル**: pro | **アスペクト比**: 16:9 | **解像度**: 4k

## プロンプト本文

```
技術解説用のメンタルモデル図を作成してください。概念 Section 冒頭のサマリー図で、一目で要点が伝わることを最優先します。

【ユーザーストーリー】
Laravel 経験者の読者として、「呼び出し側のコードは同じまま、実装だけ差し替わる」というポリモーフィズムの感覚を一目で掴みたい。なぜなら、この後の Spring DI で interface 型に実装を差し替える設計が腑に落ちる土台になるから。

【要件（描くもの）】
- 上部に見出しを大きく1行: 「呼び出しは同じ、実装を差し替えれば振る舞いが変わる」
- 左に「呼び出し側」の箱。そこから右へ1本の矢印、矢印のラベル「send()」
- 中央に受け口の箱「通知 (interface)」。下側に1つの差込口
- 下に実装パーツ「Email」「SMS」。片方が差込口にカチッとハマり、もう片方は差し替え待ち。差し替え可能を上下両矢印「差し替え可能」で示す。ハマっている方だけオレンジ
- 視線が 左→右、そして下からの差し込み で流れる。要素はこれだけ、ごちゃつかせない

【レイアウト（設計図。配置の指針としてのみ使い、罫線・記号・ASCIIの文字そのものは描かない）】
        呼び出しは同じ、実装を差し替えれば振る舞いが変わる
   [ 呼び出し側 ] ── send() ──▶ [ 通知 (interface) ]
                                       ▲ 差し替え可能
                                [ Email ]   [ SMS ]

【スタイル】
- フラットデザイン・インフォグラフィック風。影・グラデーション・写実は避ける
- 白背景(#FFFFFF)。基調色 #007396（ティール寄りの青）、ハマっている実装パーツのみ #ED8B00（濃いオレンジ）
- クリーンなアウトライン線画、大きく読みやすいラベル、16:9 横長レイアウト

【受け入れ条件】
- 読者が3秒で「1つの呼び出し口に複数の実装を差し込める」と言える
- 「呼び出し側は同じ／中身が変わる」の対比が視覚的に分かる

【重要・描く文字の制約】
画像内に描いてよい文字は次のラベルだけ: 「呼び出しは同じ、実装を差し替えれば振る舞いが変わる」「呼び出し側」「send()」「通知 (interface)」「差し替え可能」「Email」「SMS」。上記の【ユーザーストーリー】【要件】【受け入れ条件】などの説明文は設計指示であり、画像内には一切描かない。指定ラベル以外の文字・英単語・記号・ふりがな・カタカナ・ロゴ・マスコットも描かない。
```

## 生成日時

2026-06-02
