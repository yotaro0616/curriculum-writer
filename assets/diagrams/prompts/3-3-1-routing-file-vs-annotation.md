# 3-3-1-routing-file-vs-annotation

**モデル**: pro | **アスペクト比**: 16:9 | **解像度**: 4k

## プロンプト本文

```
インフォグラフィック風の対比図を作成してください。1枚を左右に分け、左を「Before（Laravel）」、右を「After（Spring）」とします。

【内容】
- 左（Before・Laravel）: 中央に1枚の大きなファイル「routes/api.php」。そこから複数の線が、周囲に散らばった3〜4個のコントローラ（メソッド名が数個並んだ箱）へ伸びる。ルート定義と処理が別々の場所にある様子。下部の見出しは「ルートを1か所に集約」
- 右（After・Spring）: コントローラのメソッドの箱が縦に4つ整然と並び、各箱の真上に「@GetMapping」「@PostMapping」「@PutMapping」「@DeleteMapping」のアノテーションが帯状のラベルとして直接貼られている。ルート定義と処理が同じ場所にある様子。下部の見出しは「メソッドの真上に宣言」
- 中央に左から右への矢印
- 画像に描く文字は、上記のラベルと見出しだけにする

【スタイル】
- フラットデザイン・インフォグラフィック風。影・グラデーション・写実表現は避ける
- 白背景。左側はグレーを基調に、右側は青(#3B82F6)とオレンジ(#F97316)を使う。「グレー寄り」などの配色を説明する文字は画像内に描かない
- クリーンなアウトラインの線画。ラベルは日本語（@GetMapping や routes/api.php などの用語は可）
- 16:9 横長レイアウト
```

## 生成日時

2026-06-03
