---
name: write
description: |
  教材の執筆ワークフロー。Part・Chapter・Section のいずれの単位でも対応する。
  新規執筆・更新・ブラッシュアップのいずれも対応。OUTLINE.md の設計に基づき、参考資料を参照しながら書き上げる。
  「2-1 を書いて」「Section 2-3-5 を更新して」「Part 1 をブラッシュアップして」「次の Section を書いて」など、教材の執筆に関する依頼で使用する。
argument-hint: "[スコープ] [追加指示(任意)]"
---

# 教材の執筆ワークフロー

**入力**: $ARGUMENTS

## スコープの解釈

CLAUDE.md の HOW セクションで定義された階層構造に基づいてスコープを判定する。

プレフィックス付きの指定を推奨する（曖昧さを排除するため）。番号のみの入力も階層構造に基づいて解釈する。

**3層**（Part > Chapter > Section）:
| 入力（推奨） | 番号のみ | 対象 |
|---|---|---|
| `全て`, `残り全部` | — | 全 Section |
| `Part 2` | `2` | Part 全体 |
| `Chapter 2-1` | `2-1` | Chapter 全体 |
| `Section 2-1-3` | `2-1-3` | Section 単体 |

**2層**（Chapter > Section）:
| 入力（推奨） | 番号のみ | 対象 |
|---|---|---|
| `全て`, `残り全部` | — | 全 Section |
| `Chapter 2` | `2` | Chapter 全体 |
| `Section 2-3` | `2-3` | Section 単体 |

**1層**（Section のみ）:
| 入力（推奨） | 番号のみ | 対象 |
|---|---|---|
| `全て`, `残り全部` | — | 全 Section |
| `Section 3` | `3` | Section 単体 |

「残り全部」の場合は `curriculums/` に既存ファイルがある Section をスキップする。

ディレクトリパスは CLAUDE.md のフォルダ構造・命名規則に従う。タイトルは OUTLINE.md から取得。

## 1. 準備

1. `CLAUDE.md` を Read し、哲学（ペルソナ・コンセプト・ゴール）と階層構造を確認
2. `OUTLINE.md` を Read し、対象スコープの設計を把握（ゴール、種類、参考資料、前提 Section 等）
3. 参考資料を取得（URL がある場合は WebFetch、必要に応じて WebSearch でリサーチ）。取得した情報から教材に含める数値・仕様・制限を箇条書きで整理し、執筆時は記憶ではなくこの整理結果を参照する
4. `curriculums/` に既存ファイルがあればサブエージェントで分析（用語の一貫性、前後との接続）
5. 実践プロジェクトがある場合: Section 間の依存関係（前の Section で作成したコード・設定を後の Section が前提とする箇所）を確認し、整合性を保って執筆する

## 2. 方針合わせ

ユーザーに判断を伝え、方向性を揃える。

- **体験設計**: この範囲で学習者にどういう体験を届けるか
- **構成判断**: 見出し構成を提案する。OUTLINE.md のスコープを変更する場合は理由を含める
- **不明点**: 判断に迷う点があれば質問する

ユーザーの反応を受けてから執筆に入る。

### 大規模スコープの場合（Part / Chapter 全体 / 教材全体）

- **方針合わせは Chapter 単位**: Chapter ごとに体験設計と、各 Section の見出し構成（1行サマリーでよい）を提示する
- **参考資料は事前に一括取得**: 整理結果を1つにまとめ、各サブエージェントに渡す（重複取得を防ぐ）
- **サブエージェントへのルール伝達**: `.claude/rules/writing.md` のパスを渡し、各エージェントに Read させる（プロンプトへの埋め込みは避ける）
- **中間チェックポイント**: Chapter 単位で執筆完了後にユーザーに報告し、方向修正の機会を設ける

## 3. 執筆

CLAUDE.md の哲学を踏まえ、`.claude/rules/writing.md` のルール・テンプレートに沿って書く。上位スコープの場合は、含まれる全 Section を順に執筆する。

## 4. セルフチェック・完了

1. 実践プロジェクトを扱う場合: 言及したコードの実在をサブエージェントで確認
2. 参考資料との照合: 教材に記載した数値・仕様が準備で整理した内容と一致するか確認
3. セルフチェック結果を報告し、`/review` の実行を提案する
